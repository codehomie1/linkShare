<article class="comment-card" id="<%= dom_id comment %>">
  <p class="comment-card__body"><%= comment.body %></p>
  <footer class="comment-card__meta">
    <%= comment.user == current_user ? "You" : comment.user.email %> · <%= time_ago_in_words(comment.created_at) %> ago
  </footer>
</article>
