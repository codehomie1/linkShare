<% content_for :title, "New link" %>

<div class="page">
  <header class="page-header">
    <h1 class="page-title">New link</h1>
    <%= link_to "← Back to links", links_path, class: "btn btn--ghost" %>
  </header>

  <div class="card">
    <%= render "form", link: @link %>
  </div>
</div>
